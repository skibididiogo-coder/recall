# Generated by "04 System/(C) build-a11y-check.mjs", which lifts the markup out of
# app.js's own render functions so the check cannot drift. It is rebuilt on demand,
# never edited by hand, and therefore never committed.
(C) a11y-check.html

# macOS
.DS_Store
